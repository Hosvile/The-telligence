pcall(function(ksodjdiishdjsjsj';$8'-_18'-4@9':924-9-'9-294-'942'+942+'9+29'_9-2+'94'94-92:'942-'84-8_-'8_9-'28_-_8'-842-'84-8'-8_2-8-_'_82'-8_2-_2'84'-8-'82-'48-8-'_9+'+:9:#+_59+597:97::075#7:9_#+9-:9_2-:8_2-+9:_#97:95#7:07027_9:79_0:702_:505#:5/##:8#&8:5#9:7#57_++_:+::_+#:+2_7:5578:59205259:9:8275-_+:2#+_:+#+#+_:7#_7::822&95:5920:50:/50:5:+_:;!#_!_'?_#!'#2!_';;'#1-$72$72_+2-_-2_;2_;_;22;_!_2!__-_37_73_9_20_04)
getgenv().Key("yes")
end)













































































































































































































































































































































































































getgenv().Ban1 = false
getgenv().NpClips = false
getgenv().corpel = false
getgenv().Atp = false
getgenv().CQueen = false
getgenv().CAlate = false
getgenv().CMonarch = false
getgenv().CEmpress = false
getgenv().CLay = false
getgenv().YQueen = false
getgenv().YAlate = false
getgenv().YMonarch = false
getgenv().YEmpress = false
getgenv().YLay = false
getgenv().FQueen = false
getgenv().FAlate = false
getgenv().FMonarch = false
getgenv().FEmpress = false
getgenv().FLay = false
getgenv().Flinging = false
getgenv().Chatpie = false
getgenv().Jayned = false
getgenv().corpel1 = false
getgenv().Ress = false
getgenv().Tepqq = false
getgenv().Tequ = false
getgenv().amu = false
getgenv().amw = false
getgenv().larves = false
getgenv().Targeka = false
getgenv().anta = false
getgenv().anta3 = false

loop1 = true
--This script reveals ALL hidden messages in the default chat
--chat "/spy" to toggle!
enabled = true
--if true will check your messages too
spyOnMyself = false
--if true will chat the logs publicly (fun, risky)
public = false
--if true will use /me to stand out
publicItalics = false
--customize private logs
privateProperties = {
    Color = Color3.fromRGB(0,255,255);
    Font = Enum.Font.SourceSansBold;
    TextSize = 18;
}
--////////////////////////////////////////////////////////////////
local neyam

local playerliests = {}

local pacha = game.Players.LocalPlayer.Character.HumanoidRootPart
local pakla = game.Players:GetDescendants()
local paklas = game.Players:GetDescendants()
local owf = game.Players:WaitForChild("AntClass",0.1)


for _, v in pairs(pakla) do

if v.Name == "AntClass" then
if v.Value == "Queen" then
print(v.Parent,"Queen")
if v.Parent == nil then
repeat
print(v,"was detroyed due to nil")
v:Destroy()
until v.Parent ~= nil
end

if v.Parent.Character:WaitForChild("Humanoid").Died then
for ia, va in pairs(playerliests) do
if va == playeri then
print(va,"whoopsie")
table.remove(playerliests,ia)
end
end
end
print(v,"pa")
table.insert(playerliests,v.Parent.Name)

end
end
end

print("66")


local remotePath1 = game:GetService("ReplicatedStorage").Remotes.Server.Morph

local flingtbl = {}
local noclip = true
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()

local char = game.Players.LocalPlayer.Character or game.Players.LocalPlayer.CharacterAdded:Wait()
local light = Instance.new("PointLight")

local rootpart = game.Players.LocalPlayer.character.HumanoidRootPart

local bv = Instance.new("BodyAngularVelocity")

local Char1 = game.Players.LocalPlayer.Character:GetChildren()

local StarterGui = game:GetService("StarterGui")
local Players = game:GetService("Players")
local playere = Players.LocalPlayer
local saymsg = game:GetService("ReplicatedStorage"):WaitForChild("DefaultChatSystemChatEvents"):WaitForChild("SayMessageRequest")
local getmsg = game:GetService("ReplicatedStorage"):WaitForChild("DefaultChatSystemChatEvents"):WaitForChild("OnMessageDoneFiltering")
local instance = (_G.chatSpyInstance or 0) + 1



Players = game:GetService("Players")

hummy = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart

local VLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/vep1032/VepStuff/main/VL"))()

local s = VLib:Window("Hostile", "Global hub", "Specific")

local ss = s:Tab("Queens")

local tt = s:Tab("Ants")

local uu = s:Tab("Spider")

local vv = s:Tab("Hostile")

local ww = s:Tab("Others")

local tt = s:Tab("Teleports")

local xx = s:Tab("UIs")

local ii = s:Tab("Tip & info")

local yy = s:Tab("Misc")

local zc = s:Tab("Credits")

local drow = vv:Dropdown("Queen dropdown",playerliests,function(t)print(t)neyam = t print(t,"ok")end)
print(neyam)

function TCB(String)
local clipBoard = setclipboard or toclipboard or set_clipboard or (Clipboard and Clipboard.set)
	if clipBoard then
		clipBoard(String)
	end
end

function Lev()
spawn(function()
    while infs == true do
    Playeree = game:GetService("Players")
    local firm = game.Players:FindFirstChild {
        "Hosvile"
    }
    for i, player in pairs(Playeree:GetPlayers()) do
    if player.Name == "Void_Giver" then
    print("he here")
    local CoreGui = game:GetService("StarterGui")

    CoreGui:SetCore("SendNotification", {
        Title = "Aquaorr Admin has joined",
        Text = "An Admin has joined! turn on chat spy to see their chat",
        Duration = 10
    })
    end
    if player.Name == "NotIntellagen_ce" then
    print("he here")
    local CoreGui = game:GetService("StarterGui")

    CoreGui:SetCore("SendNotification", {
        Title = "Aquaorr Admin has joined",
        Text = "An Admin has joined! turn on chat spy to see their chat",
        Duration = 10
    })
    end
    break
    end
    end
    print("scanning...")
    end)
end
Lev()

function CEmpress()
spawn(function()
    while getgenv().CEmpress == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Empress"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Carpenter Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait(0.0001)
    end
    end)
end

function YEmpress()
spawn(function()
    while getgenv().YEmpress == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Empress"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Yellow Crazy Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait(0.0001)
    end
    end)
end

function FEmpress()
spawn(function()
    while getgenv().FEmpress == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Empress"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Fire Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait(0.0001)
    end
    end)
end

function CQueen()
spawn(function()
    while getgenv().CQueen == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Queen"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Carpenter Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait()
    end
    end)
end

function YQueen()
spawn(function()
    while getgenv().YQueen == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Queen"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Yellow Crazy Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait()
    end
    end)
end

function FQueen()
spawn(function()
    while getgenv().FQueen == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Queen"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Fire Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait()
    end
    end)
end

function CAlate()
spawn(function()
    while getgenv().CAlate == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Alate"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Carpenter Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait()
    end
    end)
end

function FAlate()
spawn(function()
    while getgenv().FAlate == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Alate"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Fire Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait()
    end
    end)
end

function YAlate()
spawn(function()
    while getgenv().YAlate == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Alate"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Yellow Crazy Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait()
    end
    end)
end

function CMonarch()
spawn(function()
    while getgenv().CMonarch == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Monarch"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Carpenter Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait()
    end
    end)
end

function YMonarch()
spawn(function()
    while getgenv().YMonarch == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Monarch"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Yellow Crazy Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait()
    end
    end)
end

function FMonarch()
spawn(function()
    while getgenv().FMonarch == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Monarch"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Fire Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait()
    end
    end)
end

function CLay()
spawn(function()
    while getgenv().CLay == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Egg Layer"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Carpenter Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait()
    end
    end)
end

function YLay()
spawn(function()
    while getgenv().YLay == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Egg Layer"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Yellow Crazy Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait()
    end
    end)
end

function FLay()
spawn(function()
    while getgenv().FLay == true do
    local args = {
        [1] = {
            ["EggType"] = "Queen",
            ["Morph"] = "Egg Layer"
        },
        [2] = game:GetService("Teams"):FindFirstChild("Fire Ants")
    }

    remotePath1:FireServer(unpack(args))
    wait()
    end
    end)
end



function Atport()
spawn(
    function()
    while getgenv().Atp == true do
    wait(0.05)
    if
        (game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame.Y > 20 or
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame.Y < 9)
    then
    print("Y e t")
    game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = true
    wait()
    game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = false
    print("working")
    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame =
    CFrame.new(
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame.X,
        12.6,
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame.Z)
    end
    end
    end)
end

function Tweyn(t)
spawn(function()
    while getgenv().Twen == true do
    wait()
    print(t,"doesn't or")
    if hummy.Parent.Parent ~= nil then
    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = game.Players[t].Character.HumanoidRootPart.CFrame
    print(t,"works")
    end
    end
    end)
end

function Layt()
print("light works")
light.Range = 2500 --adjust as needed
light.Parent = char:WaitForChild("HumanoidRootPart")
print("yep")
end

function Flinge()
spawn(function()
    if not rootpart then return end
    flingtbl.OldVelocity = rootpart.Velocity
    flingtbl.bv = bv
    bv.MaxTorque = Vector3.new(1, 1, 1) * math.huge
    bv.P = math.huge
    bv.AngularVelocity = Vector3.new(0, 9e5, 0)
    bv.Parent = rootpart

    for i, v in next, Char1 do
    if v:IsA("BasePart") then
    v.CanCollide = false
    v.Massless = true
    v.Velocity = Vector3.new(0, 0, 0)
    print("CancollideFalse")
    break
    end
    end
    flingtbl.Noclipping2 = game:GetService("RunService").Stepped:Connect(function()
        for i, v in next, Char1 do
        if v:IsA("BasePart") then
        v.CanCollide = false
        break
        end
        end
        end)
    flinging = true
    end)
end

function Unflin()
spawn(function()
    if not rootpart then return end
    flingtbl.OldPos = rootpart.CFrame

    if flingtbl.bv ~= nil then
    flingtbl.bv:Destroy()
    flingtbl.bv = nil
    end
    if flingtbl.Noclipping2 ~= nil then
    flingtbl.Noclipping2:Disconnect()
    flingtbl.Noclipping2 = nil
    end
    for i, v in next, Char1 do
    if v:IsA("BasePart") then
    v.CanCollide = true
    v.Massless = false
    end
    end
    print("537")
    flingtbl.isRunning = game:GetService("RunService").Stepped:Connect(function()
        if flingtbl.OldPos ~= nil then
        rootpart.CFrame = flingtbl.OldPos
        print("ah")
        end
        if flingtbl.OldVelocity ~= nil then
        rootpart.Velocity = flingtbl.OldVelocity
        print("544")
        end
        end)



    wait(2)
    rootpart.Anchored = true
    if flingtbl.isRunning ~= nil then
    flingtbl.isRunning:Disconnect()
    flingtbl.isRunning = nil
    end
    rootpart.Anchored = false
    if flingtbl.OldVelocity ~= nil then
    rootpart.Velocity = flingtbl.OldVelocity
    end
    if flingtbl.OldPos ~= nil then
    rootpart.CFrame = flingtbl.OldPos
    end
    wait()
    flingtbl.OldVelocity = nil
    flingtbl.OldPos = nil
    flinging = false
    end)
end

function ABan()
spawn(function()
    while getgenv().Ban1 == true do
    setfflag("DFStringCrashPadUploadToBacktraceToBacktraceBaseUrl", "")
    setfflag("DFIntCrashUploadToBacktracePercentage", "0")
    setfflag("DFStringCrashUploadToBacktraceBlackholeToken", "")
    setfflag("DFStringCrashUploadToBacktraceWindowsPlayerToken", "")
    wait(0.25)
    print("Ban")
    end
    end)
end

function NClip()
spawn(function()
    while getgenv().NpClips == true do
    player = game.Players.LocalPlayer
    character = player.Character

    if noclip then
    for _, v in pairs(character:GetDescendants()) do
    pcall(function()
        if v:IsA("BasePart") then
        v.CanCollide = false
        end
        end)
    end
    end

    game:GetService("RunService").Stepped:wait()
    end
    end)
end

function Leavt()
spawn(function()
    while getgenv().Jayned == true do
    local annoying = {
        "Aquaorr","Void_Giver"
    } -- you can add more players by doing {"Player1", "Player2"} etc
    wait()
    game.Players.PlayerAdded:Connect(function(plr)
        for i, v in pairs(annoying) do
        if plr.Name == v then
        local loc = game.Players.LocalPlayer
        loc:Kick("Admin Joined")
        end
        end
        end)
    end
    end)
end

function Skyiee()
spawn(function()
    local Skycount = 0
    local Stepp = game.Players.LocalPlayer.Character
    repeat
    game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = true
    Stepp.HumanoidRootPart.CFrame = CFrame.new(905, 588, 433)
    Stepp.HumanoidRootPart.Anchored = false
    wait()
    Stepp.HumanoidRootPart.Anchored = true
    Stepp.HumanoidRootPart.CFrame = CFrame.new(905, 588, 433)
    wait()
    game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = false
    wait()
    game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = true
    Stepp.HumanoidRootPart.CFrame = CFrame.new(905, 588, 433)
    Stepp.HumanoidRootPart.Anchored = false
    wait()
    Stepp.HumanoidRootPart.Anchored = true
    Stepp.HumanoidRootPart.CFrame = CFrame.new(905, 588, 433)
    wait()
    game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = false
    Skycount = Skycount + 1
    until Skycount > 3
    print("Yeet")
    end)
end

function Watuer()
spawn(function()
    local Watuo = 0
    local Stepp = game.Players.LocalPlayer.character
    repeat
    game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = true
    Stepp.HumanoidRootPart.CFrame = CFrame.new(-474, 40, 464)
    Stepp.HumanoidRootPart.Anchored = false
    wait()
    Stepp.HumanoidRootPart.Anchored = true
    Stepp.HumanoidRootPart.CFrame = CFrame.new(-474, 40, 464)
    wait()
    game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = false
    wait()
    game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = true
    Stepp.HumanoidRootPart.CFrame = CFrame.new(-474, 40, 464)
    Stepp.HumanoidRootPart.Anchored = false
    wait()
    Stepp.HumanoidRootPart.Anchored = true
    Stepp.HumanoidRootPart.CFrame = CFrame.new(-474, 40, 464)
    wait()
    game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = false
    Watuo = Watuo + 1
    until Watuo > 3
    if Stepp.HumanoidRootPart.CFrame ~= CFrame.new(-474, 31, 464) then

    end
    end)
end

function Corpe1()
spawn(function()
    local corpsess = game.Workspace.Food.Leaf.Leaf
    local corpea = game.Workspace.Food.Leaf.Pickable
    local getlef = game.Workspace.Food:GetChildren()
    local leff = getlef.Leaf
    local corpee = game.Workspace.Food.Leaf.Pickable:FindFirstChild("Picked")
    local corpeq = game.Workspace.Food.Leaf.Pickable
    local loce = game.Players.LocalPlayer.Character.HumanoidRootPart
    local corpen = game.Workspace.Food.Leaf.Pickable.Uses
    local coen = 0
    local coens = 0

    for i, v in pairs(getlef) do
    if corpen.Value == 25 then
    repeat
    if not v:FindFirstChild("Leaf") then
    repeat
    print(v,"was destroyed")
    v:Destroy()
    coens = coens + 1
    until coens > 4
    end
    print("haw")
    loce.CanCollide = false
    loce.Anchored = true
    loce.CFrame = v.Leaf.CFrame
    wait(0.1)
    loce.CFrame = v.Leaf.CFrame
    loce.Anchored = false
    wait(0.2)
    coen = coen + 1
    until coen > 1
    loce.Anchored = false
    else
        print("hewwa")
    v:Destroy()
    print("not leaf")
    end
    wait(0.2)
    loce.CanCollide = true
    print("heaa")
    break
    end
    end)
end

function Tepq(tal)
spawn(function()
    print(tal,"eyay")
    local plek = game.Players.LocalPlayer.Character.HumanoidRootPart
    while getgenv().Tepqq == true do
    wait(0.1)
    print(tal)
    plek.Anchored = true
    plek.CFrame = tal.Character.HumanoidRootPart.CFrame
    print("hskso")
    wait(0.2)
    plek.Anchored = false
    end
    end)
end

vewi = false
function viewa(tk)
vewi = tk
if vewi == false then
workspace.CurrentCamera.CameraSubject = game.Players.LocalPlayer.Character
end
end

function Spk()
spawn(function()
    pcall(function()
        while getgenv().amu == true do
        wait(1.5)
        if t == false then
        vewi = false
        game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = false
        break
        end
        ajsis = true
        for _, v in pairs(game.Players:GetDescendants()) do
        if v.Name == "AntClass" then
        if v.Value == "Queen" then
        local Players = game:GetService("Players")
        local Teams = game:GetService("Teams")
        local luca = game.Players.LocalPlayer
        local geq = game.Players:GetPlayers()
        for i, playerk in pairs(geq) do
        if playerk.Team == Players.LocalPlayer.Team then
        else
            if playerk == v.Parent then
        if not playerk:FindFirstChild("Blocking") then
        if playerk.Character:FindFirstChild("ForceField") then
        else
            local lopa = 0
        local lopca = 0
        spawn(function ()
            local pasye = 0
            local viel
            local pasyi = game.Players.LocalPlayer
            if vewi == true then
            workspace.CurrentCamera.CameraSubject = playerk.Character
            else
                workspace.CurrentCamera.CameraSubject = pasyi.Character
            end
            repeat
            wait()
            local pasy = game.Players.LocalPlayer.Character.HumanoidRootPart
            pasy.CanCollide = false
            pasy.Anchored = true
            pasy.CFrame = playerk.Character.HumanoidRootPart.CFrame * CFrame.new(0,-3,11)
            pasy.Anchored = false
            local args = {
                [1] = workspace.Terrain,
                [2] = true
            }

            game:GetService("ReplicatedStorage").Remotes.Server.Attacks.Bite:FireServer(unpack(args))
            pasye = pasye + 1
            local pasy = game.Players.LocalPlayer.Character.HumanoidRootPart
            pasy.Anchored = true
            pasy.CFrame = playerk.Character.HumanoidRootPart.CFrame * CFrame.new(0,-3,11)
            pasy.Anchored = false
            until pasye == 4
            local pasy = game.Players.LocalPlayer.Character.HumanoidRootPart
            wait()
            pasy.Anchored = true
            pasy.CFrame = CFrame.new(500,600,500)
            pasy.Anchored = false
            local poes = 0
            repeat
            wait()
            pasy.CanCollide = false
            pasy.Anchored = true
            pasy.CFrame = playerk.Character.HumanoidRootPart.CFrame * CFrame.new(0,-3,11)
            poes = poes + 1
            until poes == 1
            wait()
            pasy.CFrame = CFrame.new(500,600,500)
            pasy.CanCollide = true
            pasy.Anchored = false
            end)
        end
        end
        end
        end
        end
        end
        end
        end
        end
        end)
    end)
end


local teap = ""


function zka(teap)
print("work pls")
spawn(function()
    while getgenv().Targeka == true do
    wait(1.5)
    ajsis = true
    for _, v in pairs(game.Players:GetDescendants()) do
    if v.Name == "AntClass" then
    if v.Value == "Queen" then
    else
        print("wka?")
    local Players = game:GetService("Players")
    local Teams = game:GetService("Teams")
    local luca = game.Players.LocalPlayer
    local geq = game.Players:GetPlayers()
    for i, playerk in pairs(geq) do
    if playerk.Team == Teams[teap] then
    if playerk == v.Parent then
    if not playerk:FindFirstChild("Blocking") then
    if not playerk.Character:FindFirstChild("ForceField") then
    local lopa = 0
    local lopca = 0
    spawn(function ()
        local pasye = 0
        local viel
        local pasyi = game.Players.LocalPlayer
        if vewi == true then
        workspace.CurrentCamera.CameraSubject = playerk.Character
        else
            workspace.CurrentCamera.CameraSubject = pasyi.Character
        end
        repeat
        wait()
        local pasy = game.Players.LocalPlayer.Character.HumanoidRootPart
        pasy.CanCollide = false
        pasy.Anchored = true
        pasy.CFrame = playerk.Character.HumanoidRootPart.CFrame * CFrame.new(0,-2,8)
        pasy.Anchored = false
        local args = {
            [1] = workspace.Terrain,
            [2] = true
        }

        game:GetService("ReplicatedStorage").Remotes.Server.Attacks.Bite:FireServer(unpack(args))
        pasye = pasye + 1
        local pasy = game.Players.LocalPlayer.Character.HumanoidRootPart
        pasy.Anchored = true
        pasy.CFrame = playerk.Character.HumanoidRootPart.CFrame * CFrame.new(0,-2,8)
        pasy.Anchored = false
        until pasye == 4
        local pasy = game.Players.LocalPlayer.Character.HumanoidRootPart
        wait()
        pasy.Anchored = true
        pasy.CFrame = CFrame.new(500,600,500)
        pasy.Anchored = false
        local poes = 0
        repeat
        wait()
        pasy.CanCollide = false
        pasy.Anchored = true
        pasy.CFrame = playerk.Character.HumanoidRootPart.CFrame * CFrame.new(0,-2,8)
        poes = poes + 1
        until poes == 1
        wait()
        pasy.CFrame = CFrame.new(500,600,500)
        pasy.CanCollide = true
        pasy.Anchored = false
        end)
    end
    end
    end
    end
    end
    end
    end
    end
    end
    end)
end


function vel()
spawn(function()
    for _, v in pairs(game.Players:GetDescendants()) do
    if v.Name == "AntClass" then
    local Players = game:GetService("Players")
    local Teams = game:GetService("Teams")
    local luca = game.Players.LocalPlayer
    local geq = game.Players:GetPlayers()
    local wala = 0
    if v.Parent.Team == Teams["Lobby"] then
    print(v.Parent.Team,"hi")
    else
        repeat
    luca.Character.HumanoidRootPart.Anchored = true
    luca.Character.HumanoidRootPart.CFrame = v.Parent.Character.HumanoidRootPart.CFrame * CFrame.new(0,0,2)
    luca.Character.HumanoidRootPart.Anchored = false
    wait(0.02)
    luca.Character.HumanoidRootPart.Anchored = true
    luca.Character.HumanoidRootPart.CFrame = v.Parent.Character.HumanoidRootPart.CFrame * CFrame.new(0,0,2)
    luca.Character.HumanoidRootPart.Anchored = false
    wala = wala + 1
    until wala > 3
    end
    end
    local lucas = game.Players.LocalPlayer
    local walae = 0
    repeat
    lucas.Character.HumanoidRootPart.Anchored = true
    lucas.Character.HumanoidRootPart.CFrame = CFrame.new(400,625,400)
    wait()
    lucas.Character.HumanoidRootPart.CanCollide = true
    lucas.character.HumanoidRootPart.Anchored = false
    walae = walae + 1
    until walae > 1
    end
    end)
end

function spi()
spawn(function()
    for _, v in pairs(game.Players:GetDescendants()) do
    if v.Name == "AntClass" then
    local gewa = game.Players.LocalPlayer
    local Teams = game:GetService("Teams")
    if v.Parent.Team == gewa.Team then
    else
        if wuia > 1 then
    break
    end
    local wuia = 0
    local lucs = game.Players.LocalPlayer.Character.HumanoidRootPart
    lucs.Anchored = true
    lucs.CFrame = v.Parent.Character.HumanoidRootPart.CFrame
    lucs.Anchored = false
    wait(0.5)
    lucs.Anchored = true
    lucs.CFrame = v.Parent.Character.HumanoidRootPart.CFrame
    lucs.Anchored = false
    wait(5)
    lucs.Anchored = true
    lucs.CFrame = CFrame.new(500,650,500)
    lucs.Anchored = false
    lucs.Anchored = false
    wuia = wuia + 1
    lucs.Anchored = false
    end
    end
    end
    end)
end

print("504")

function spesyal()
spawn(function()
    local gewaa = game.Players.LocalPlayer.Character.HumanoidRootPart
    gewaa.Anchored = true
    gewaa.CFrame = CFrame.new(-2147483648, 2147483648, 2147483648)
    gewaa.Anchored = false
    wait()
    gewaa.Anchored = true
    gewaa.CFrame = CFrame.new(-2147483648, 2147483648, 2147483648)
    gewaa.Anchored = false
    wait()
    gewaa.Anchored = true
    gewaa.CFrame = CFrame.new(-2147483648, 2147483648, 2147483648)
    gewaa.Anchored = false
    end)
end



function larve()
spawn(function()

    while getgenv().larves == true do
    wait(1)
    for i, v in pairs(game.Workspace.Eggs:GetDescendants()) do
    if v.Name == "Larvae" then
    if v:FindFirstChild("Body") then
    local Players = game:GetService("Players")
    local Teams = game:GetService("Teams")
    if v.Team.Value == Players.LocalPlayer.Team then
    else
        local playak = game.Players.LocalPlayer.Character.HumanoidRootPart
    local paske = 0
    repeat
    wait()
    local pasy = game.Players.LocalPlayer.Character.HumanoidRootPart
    pasy.CanCollide = false
    pasy.Anchored = true
    pasy.CFrame = v.Body.CFrame * CFrame.new(0,2,0)
    pasy.Anchored = false
    local args = {
        [1] = workspace.Terrain,
        [2] = true
    }

    game:GetService("ReplicatedStorage").Remotes.Server.Attacks.Bite:FireServer(unpack(args))
    paske = paske + 1
    local pasy = game.Players.LocalPlayer.Character.HumanoidRootPart
    pasy.Anchored = true
    pasy.CFrame = v.Body.CFrame * CFrame.new(0,2,0)
    pasy.Anchored = false
    until paske == 6
    end
    end
    else
        end
    end
    end
    end)
end

function skplr()
spawn(function()
    local skaks = Instance.new("Folder")

    end)
end

local anta2 = false
function sken2()
spawn(function()
    if anta2 == false then
    anta2 = true
    local pas = Instance.new("Folder")
    pas.Name = "AntAbilities"
    pas.Parent = game.Players.LocalPlayer
    else
        return
    end
    end)
end
sken2()

function skena()
spawn(function()
    for i, vq in pairs(game.Players:GetPlayers()) do
    if vq:FindFirstChild("AntAbilities") then
    print(vq.Name)
    if getgenv().anta3 == false then
    for i, vy in pairs(game.Workspace:GetDescendants()) do
    if vy.Name == "AntUi" then
    vy:Destroy()
    end
    end
    end
    if getgenv().anta == true then
    print("trru")
    return
    end
    print("shaet")
    getgenv().anta = true
    local plr = game:GetService("Workspace")
    local target = plr[vq.Name]
    local targett = target.Name
    local targs = vq.Name
    local BillboardGui = Instance.new("BillboardGui")
    local TextLabel = Instance.new("TextLabel")
    BillboardGui.Adornee = vq.Character.Head
    BillboardGui.Name = "AntUi"
    BillboardGui.Parent = vq.Character
    BillboardGui.Size = UDim2.new(0, 100, 0, 150)
    BillboardGui.StudsOffset = Vector3.new(0, 1, 0)
    BillboardGui.AlwaysOnTop = true
    TextLabel.Parent = BillboardGui
    TextLabel.BackgroundTransparency = 1
    TextLabel.Position = UDim2.new(0, 0, 0, -50)
    TextLabel.Size = UDim2.new(0, 100, 0, 100)
    TextLabel.Font = Enum.Font.SourceSansSemibold
    TextLabel.TextSize = 20
    TextLabel.TextColor3 = Color3.new(1, 1, 1)
    TextLabel.TextStrokeTransparency = 0
    TextLabel.TextYAlignment = Enum.TextYAlignment.Bottom
    TextLabel.Text = 'S.User: '..targs
    TextLabel.ZIndex = 10
    print("suce?")
    end
    end
    end)
end

function qespy()
    spawn(function()
        
        local countant = 0
        local rempla
local teamq1

local carpt = ""
local firet = ""
local crazyt = ""
local tesa1 = ""

        for i, vq in pairs(game.Players:GetPlayers()) do

if vq ~= game.Players.LocalPlayer then
    if vq:FindFirstChild("AntClass") then
if vq.AntClass.Value ~= "Queen" then
if vq.Team == carpt or firet or crazyt then
local teampla = ""
local teampla2 = vq.Team
teampla = teampla2
teamq1 = teampla2
countant = countant + 1
rempla = countant
print(rempla)
print(teampla)
end
end
print(teamq1,"is ant")
if vq.AntClass.Value == "Queen" then
print(vq,"person")
teamq1 = vq.Team
for b,n in pairs (vq.Character:GetChildren()) do
if t == false then
print("ok gotit")
vq.Character.AntBoxer:Destroy()
end
if vq.Team == carpt or firet or crazyt then
local teamq2 = carpt or firet or crazyt
local tesa1
print(tesa1,"ok test")
print(vq,vq.Team,"okteam")



				if (n:IsA("BasePart")) then
					local a = Instance.new("BoxHandleAdornment")
					a.Name = "AntBoxer"
					a.Parent = vq.Character
					a.Adornee = n
					a.AlwaysOnTop = true
					a.ZIndex = 10
					a.Size = n.Size
					a.Transparency = 0.5
					a.Color = vq.TeamColor
				end
			end
print(vq,"ha ok")
if n.Parent:FindFirstChild("AntUi2") then
print("nothing")
else
if not n.Parent:FindFirstChild("AntUi2") then
local teams = game:GetService("Teams"):GetTeams()
local tesa2 = ""
for _, team in pairs(teams) do
if team == vq.Team then
local players = team:GetPlayers()
print(team, "shall it?")

tesa2 = #players..' '..team.Name
    
    
    end
end
    local plr = game:GetService("Workspace")
    local target = plr[vq.Name]
    local targett = target.Name
    local targs = vq.Name
    local BillboardGui = Instance.new("BillboardGui")
    local TextLabel = Instance.new("TextLabel")
    BillboardGui.Adornee = vq.Character.Head
    BillboardGui.Name = "AntUi2"
    BillboardGui.Parent = vq.Character
    BillboardGui.Size = UDim2.new(0, 100, 0, 150)
    BillboardGui.StudsOffset = Vector3.new(0, 1, 0)
    BillboardGui.AlwaysOnTop = true
    TextLabel.Parent = BillboardGui
    TextLabel.BackgroundTransparency = 1
    TextLabel.Position = UDim2.new(0, 0, 0, -50)
    TextLabel.Size = UDim2.new(0, 100, 0, 100)
    TextLabel.Font = Enum.Font.SourceSansSemibold
    TextLabel.TextTransparency = 0.3
    TextLabel.TextSize = 12
    
    TextLabel.TextStrokeTransparency = 0
    TextLabel.TextYAlignment = Enum.TextYAlignment.Bottom
print(carpt,firet,crazyt,"hi")
TextLabel.TextColor3 = Color3.new(1,1,1)
TextLabel.Text = 'Queen: '..targs..' with '..tesa2
    TextLabel.ZIndex = 10
    print(vq)
    end
  end
end
    end
end
    end
end
    end)
end

function spj()
    spawn(function()
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 25
    end)
    end

function jpj()
    spawn(function()
        game.Players.LocalPlayer.Character.Humanoid.JumpPower = 60
    end)
    end

ww:Button("Console for Debug",function()
    StarterGui:SetCore("DevConsoleVisible",true)
    infs = true
end)

vv:Button("Scan Queen list | doesn't work",function()
    for _, v in pairs(game.Players:GetDescendants()) do
    if v.Name == "AntClass" then
    if v.Value == "Queen" then
    local Players = game:GetService("Players")
    local Teams = game:GetService("Teams")
    local luca = game.Players.LocalPlayer
    local geq = game.Players:GetPlayers()
    table.clear(playerliests)
    print(playerliests)
    table.insert(playerliests,v.Parent)
    neyam = playerliests
    print(neyam)
    end
    end
    end
    print(playerliests)
    drow:Refresh(playerliests)
    end)

ww:Button("Speedy ant",function()
    spj()
    end)

ww:Button("High jump",function()
    jpj()
    end)


vv:Button("tp to a special place",function()
    spesyal()
    end)

vv:Button("tp to an ant, useful for spy",function()
    spi()
    end)

tt:Button("Sky",function()
    Skyiee()
    end)

tt:Button("Food or Water",function()
    Watuer()
    print(neyam)
    end)

xx:Button("Chat-$py",function()
    spawn(function()
        local Revpyi = loadstring(game:HttpGet('https://pastebin.com/raw/FCdf1iB9'))()
        Revpyi = true
        if Revpyi then
        game:GetService("StarterGui"):SetCore("SendNotification", {

            Title = "/SPY ENABLED (CTTO)", -- Required

            Text = "Type /spy to toggle",
            Duration = 40 -- Required -- Optional
        })
        end
        end)
    end)

xx:Button(
    "Reviz admin",
    function()
    spawn(function()
        local Revaz = loadstring(game:HttpGet('https://pastebin.com/raw/8XrRYkHs'))()
        Revaz = true
        end)
    end)

xx:Button("Infinite Yield v5.5",function()
    spawn(function()
        local IFY = loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
        IFY = true
        end)
    end)


ww:Button("Radiate light",function()
    Layt()
    end)


vv:Button("Spin/Fling | Tip: No-clip",function()
    Flinge()
    end)

vv:Button("Unspin",function()
    Unflin()
    end)

ww:Button("Un-anchor",function()
    game.Players.LocalPlayer.Character.HumanoidRootPart = false
    end)

tt:Button("Tp to a Leaf food",function()
    Corpe1()

    print("Tp to corpses on")
    end)

vv:Button("Teleport to all, useful with Fling",function()
    vel()
    end)

ww:Button("Let other users scan you",function()
    sken2()
end)

vv:Toggle("Queen ESP",function(t)
    if t == true then
    qespy(t)
    else
for i, v in pairs(game.Workspace:GetDescendants()) do
if v.Name == "AntBoxer" then
v:Destroy()
end
if v.Name == "AntUi2" then
v:Destroy()
end
end
    end
    end)

ww:Toggle("Scan for players that use the Script",function(t)
    if t == true then
    skena()
    else
        if t == false then
    for i, v in pairs(game:GetDescendants()) do
    if v.Name == "AntUi" then
    v:Destroy()
    end
    end
    end
    end
    end)

vv:Toggle("Target a Ant by their team",function(t)
    getgenv().Targeka = t

    if t == true then
    zka(teap)
    end
    end)

vv:Toggle("Target Larvae",function(t)
    getgenv().larves = t
    if t then
    larve()
    end
    end)

vv:Toggle("View target",function(t)
    viewa(t)
    end)

vv:Toggle("Bite tp to random enemy queen",function(t)
    getgenv().amu = t
    if t then
    Spk()
    end
    end)

ww:Toggle("No-Clip",function(t)
    getgenv().NpClips = t
    if t then
    NClip()
    end
    print("No clip on")
    end)

ww:Toggle("Anti-ban | may not work",function(t)
    getgenv().Ban1 = t
    if t then
    Aban()
    end
    end)

ww:Toggle("Leave when a powerful person join",function(t)
    getgenv().Jayned = t
    if t then
    Leavt()
    end
    end)

ww:Toggle("Allow teleport | Very reliable!",
    function(t)
    getgenv().Atp = t
    if t then
    Atport()
    end
    print("Port works")
    end)

ss:Toggle("Carpenter Empress",function(t)

    getgenv().CEmpress = t

    if t then
    CEmpress();
    end
    end)

ss:Toggle("Yellow Empress",function(t)
    getgenv().YEmpress = t
    if t then
    YEmpress();
    end
    end)

ss:Toggle("Fire Empress",function(t)
    getgenv().FEmpress = t
    if t then
    FEmpress();
    end
    end)

ss:Toggle("Carpenter Queen",function(t)
    getgenv().CQueen = t
    if t then
    CQueen();
    end
    end)

ss:Toggle("Yellow Queen",function(t)
    getgenv().YQueen = t
    if t then
    YQueen();
    end
    end)



ss:Toggle("Fire Queen",function(t)
    getgenv().FQueen = t
    if t then
    FQueen();
    end
    end)

ss:Toggle("Carpenter Alate",function(t)
    getgenv().CAlate = t
    if t then
    CAlate();
    end
    end)

ss:Toggle("Yellow Alate",function(t)
    getgenv().YAlate = t
    if t then
    YAlate();
    end
    end)

ss:Toggle("Fire Alate",function(t)
    getgenv().FAlate = t
    if t then
    FAlate();
    end
    end)

ss:Toggle("Carpenter Monarch",function(t)
    getgenv().CMonarch = t
    if t then
    CMonarch();
    end
    end)

ss:Toggle("Yellow Monarch",function(t)
    getgenv().YMonarch = t
    if t then
    YMonarch();
    end
    end)

ss:Toggle("Fire Monarch",function(t)
    getgenv().FMonarch = t
    if t then
    FMonarch();
    end
    end)

ss:Toggle("Carpenter Egg Layer",function(t)
    getgenv().CLay = t
    if t then
    CLay();
    end
    end)

ss:Toggle("Yellow Egg Layer",function(t)
    getgenv().YLay = t
    if t then
    YLay();
    end
    end)

ss:Toggle("Fire Egg Layer",function(t)
    getgenv().FLay = t
    if t then
    FLay();
    end
    end)

vv:Dropdown("Team to target", {
    "Carpenter Ants","Fire Ants","Yellow Crazy Ants"
},function(t)
    for i, v in pairs(game.Players:GetPlayers()) do
    local Team1 = game.Teams

    if t == t then
    teap = t
    zka(teap)
    return
    end
    end
    end)



ww:Textbox("Tween to player | Not reliable!",true,function(t)
    Textbox.FocusLost:connect(function()
        for i,v in pairs(game.Players:GetChildren()) do
        if (string.sub(string.lower(v.Name),1,string.len(Textbox.Text))) == string.lower(Textbox.Text) then
        Textbox.Text = v.Name

        Tweyn(t)
        end
        end
        end)
    end)

ii:Label("If tp destination doesn't work, try tapping it multiple times!")

ii:Label("Tp to leaf only teleports you to 25 uses, eat once to change target")

ii:Label("Allow Teleport to teleport")

ii:Label("Use infinite yield for noclip, esp, and fling for better performance")

ii:Label("Tap Spin/Fling 2-3x for more power i guess")

ii:Label("Use Reviz Admin for tween/teleport")

ii:Label("Use fling on Reviz admin to teleport to player")

zc:Button("Copy my discord server",function()
    TCB(https://discord.gg/kZRcyF4NAd)
    end)
